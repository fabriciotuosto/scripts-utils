to use this scripts you need to import these functions into your current shell

in bash is like these
# Function helpers for svn
$SCRIPT_DIR=~/scripts/
if [ -d $SCRIPT_DIR ]; then
	for SCRIPT in $SCRIPT_DIR
	do
    . $SCRIPT
  done
fi
